* HEX_BOOSTER
  - representative hexagonal booster
  - should it be vertical? it's the only one that's not
  - modules
    * makeHexBooster(HEX_PYRAMID_NUM)
      - hexMaxPyramid()
  - OpenSCAD: works, rotated off top
  - FreeCAD: 
* PENT_BOOSTER
  - representative pentagonal booster
  - modules
    * makePentBooster(HEX_PYRAMID_NUM)
      - pentMaxPyramid()
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* HEX_FAST_LENS
  - representative hexagonal fast lens
  - modules
    * makeHexFastLensComponent()
      - makeHexFastLensFull()
        * makeHexSlowLensComponent()
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* PENT_FAST_LENS
  - representative pentagonal fast lens
  - modules
    * makePentFastLensComponent()
      - makePentFastLensFull()
        * makePentSlowLensComponent()
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* HEX_SLOW_LENS
  - representative hexagonal slow lens
  - bottom is large, trim in F360
  - modules
    * makeHexSlowLensComponent()
      - makeMaxPyramid(HEX_PYRAMID_NUM)
      - hexSlowLens() // generated by slowLens.py
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* PENT_SLOW_LENS
  - representative pentagonal slow lens
  - bottom is large, trim in F360
  - modules
    * makePentSlowLensComponent()
      - makeMaxPyramid(PENT_PYRAMID_NUM)
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* HEX_LENS
  - includes both fast and slow lens hexagonal components
  - bottom is untrimmed
  - modules
    * makeCompoundHexLens()
      - makeHexSlowLensComponent()
      - makeHexFastLensComponent()
  - OpenSCAD: works, vertical top 
  - FreeCAD: 
* PENT_LENS
  - includes both fast and slow lens pentagonal components
  - bottom is untrimmed
  - modules
    * makeCompoundPentLens()
      - makePentSlowLensComponent()
      - makePentFastLensComponent()
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* HEX_LENS_EXT
  - external of combined both lens hexagonal components
  - modules
    * makeHexFastLensFull()
      - hexMaxPyramid(...)
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* PENT_LENS_EXT
  - external of combined both lens pentagonal components
  - modules
    * makePentFastLensFull()
      - pentMaxPyramid(...)
  - OpenSCAD: works, vertical top
  - FreeCAD: 
* BOOSTERS
  - all boosters (hexagonal and pentagonal)
  - modules
    * makeBoosters()
      - makeHexBoosters()
        * h = hexPyramidNums()
        * makeHexBooster(h)
      - makePentBoosters()
        * p = pentPyramidNums()
        * makePentBooster(p)
  - OpenSCAD: works
  - FreeCAD: 
* HEX_SLOW_LENSES
  - all hexagonal slow lens components
  - modules
    * makeHexSlowLenses()
      - h = hexPyramidNums()
      - makeSlowLens(h)
        * hexSlowLens()
        * makeMaxPyramid()
  - OpenSCAD: works
  - FreeCAD: 
* PENT_SLOW_LENSES
  - all pentagonal slow lens components
  - modules
    * makePentSlowLenses()
      - p = pentPyramidNums()
      - makeSlowLens(p)
        * pentSlowLens()
        * makeMaxPyramid()
  - OpenSCAD: works 
  - FreeCAD: 
* FAST_LENSES
  - all fast lenses (hexagonal and pentagonal)
  - modules
    * makeFastLenses()
      - makeHexFastLenses()
        * h = hexPyramidNums()
        * makeHexFastLens(h)
          - makeFastLens(h)
            * ?
      - makePentFastLenses()
        * p = pentPyramidNums()
        * makePentFastLens(p)
          - makeFastLens(p)
            * ?
  - OpenSCAD: 
  - FreeCAD: 
* SLOW_LENSES
  - all slow lenses (hexagonal and pentagonal)
  - modules
    * makeSlowLenses()
      - makePentSlowLenses()
      - makeHexSlowLenses()
  - OpenSCAD: works
  - FreeCAD: 
* LENSES
  - a representative of all lenses (both fast and slow, hexagonal and pentagonal)
  - modules
    * makeLenses()
      - makeHexSlowLensComponent()
      - makeHexFastLensComponent()
      - makePentSlowLensComponent()  // TBD
      - makePentFastLensComponent()  // TBD
  - OpenSCAD: works, all overlaid vertical top
  - FreeCAD: 
